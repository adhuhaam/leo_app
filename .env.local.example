# Copy to `.env.local` and fill in your two secrets:
#   copy .env.local.example .env.local
#
# DATABASE password: Supabase → Project Settings → Database (or reset password there)
# JWT secret:        Supabase → Project Settings → API → JWT Secret

DATABASE_URL=postgresql://postgres.tdrzgemsyymsakigbazn:YOUR_DB_PASSWORD@aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres
SUPABASE_JWT_SECRET=YOUR_JWT_SECRET

# Optional — promote this email to super_admin when you run: pnpm dev:supabase:seed
# SEED_SUPER_ADMIN_EMAIL=you@yourcompany.com
